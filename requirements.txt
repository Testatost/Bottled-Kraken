# Bottled Kraken v3.4 Windows 10/11 runtime dependencies
# Target Python: Python 3.13 x64; Python 3.12 x64 remains accepted by the build script.
#
# Release builds use requirements-lock-windows.txt for reproducibility. The build
# script installs kraken==7.0.3 separately with --no-deps. coremltools bleibt
# ausdrücklich enthalten, weil Kraken .mlmodel-Dateien darüber einliest.

--extra-index-url https://download.pytorch.org/whl/cpu

# -------------------------------------------------
# Core OCR / HTR runtime used by kraken
# -------------------------------------------------
torch==2.10.0+cpu
torchvision==0.25.0+cpu
jsonschema
lxml
requests
click>=8.1,<8.3
numpy
Pillow>=9.2.0
regex
scipy~=1.15.3
protobuf>=3.0.0
coremltools>=9.0,<10
jinja2~=3.1.6
scikit-learn~=1.7.2
scikit-image~=0.25.2
shapely~=2.1.2
pyarrow
htrmopo>=0.5,~=0.5
lightning==2.6.1
torchmetrics>=1.1.0
threadpoolctl~=3.6.0
platformdirs
rich<14.1.0
safetensors~=0.7.0
iso639-lang
packaging

# -------------------------------------------------
# GUI / image / document handling
# -------------------------------------------------
PySide6
PyMuPDF
reportlab

# -------------------------------------------------
# Audio input / microphone support
# -------------------------------------------------
sounddevice

# -------------------------------------------------
# Local Whisper / Hugging Face / language-model support
# -------------------------------------------------
huggingface_hub
faster-whisper
ctranslate2
transformers
accelerate
tokenizers
tiktoken

# -------------------------------------------------
# DOCX / XML export support
# -------------------------------------------------
python-docx

setuptools<81
backports.tarfile
